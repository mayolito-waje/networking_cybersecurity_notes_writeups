### OSPF Cost

* OSPF's metric is called **cost** which is automatically calculated based on interface's bandwidth (speed).
* It is calculated by dividing a **reference bandwidth** (default of **100 mbps**) by the interface's bandwidth.
  * **Reference:** 100 mbps / **Interface:** 10 mbps = cost of **10**
  * **Reference:** 100 mbps / **Interface:** 100 mbps = cost of **1**
  * **Reference:** 100 mbps / **Interface:** 1000 mbps = cost of **1**??
  * **Reference:** 100 mbps / **Interface:** 10000 mbps = cost of **1**??
* All values *less than 1* will be converted to 1. Therefore, FastEthernet, Gigabit Ethernet, 10Gig Ethernet, etc. are equal and all have a cost of 1 by default.

![ 250](../_assets/Pasted%20image%2020260617045909.png)

Let's check R3's `f2/0` OSPF cost using the command `show ip ospf interface f2/0`:

![ 500](../_assets/Pasted%20image%2020260617050007.png)

Let's check R3's `g0/0` interface:

![ 500](../_assets/Pasted%20image%2020260617050109.png)

---

### Changing The Reference Bandwidth

You can see that the cost is the same which is 1. Clearly, the default cost configuration is not ideal!

* You can (and should!) change the reference bandwidth with this command:
  `R1(config-router)# auto-cost reference-bandwidth *megabits-per-second*`

![ 500](../_assets/Pasted%20image%2020260617050529.png)

* The command is entered in megabits per second (default is 100)
* 100000 / 100 = cost of 1000 for FastEthernet
  100000 / 1000 = cost of 100 for Gig Ethernet
* You should configure a reference bandwidth greater than the fastest links in your network (to allow for future upgrades).
* ***You should also configure the same reference bandwidth on all OSPF routers in the network.***

---

### OSPF Cost (Continuation)

![ 300](../_assets/Pasted%20image%2020260617051016.png)

* The OSPF cost to a destination is the total cost of the 'outgoing/exit interfaces'.
* For example, R1's cost to reach `192.168.4.0/24` is:
  100 (R1 G0/0) + 100 (R2 G1/0) + 100 (R4 G1/0) = **300**
* Loopback interfaces have a cost of 1
* What is R1's cost to reach `2.2.2.2` (R2's loopback0 interface)?
  100 (R1 G0/0) + 1 (R2 L0) = **101**

Here is R1's routing table **before changing the reference bandwidth**:

![ 500](../_assets/Pasted%20image%2020260617051501.png)

Notice that R1 have 2 routes to reach `192.168.4.0/24`, one via R2 and one via R3.

Here is R1's routing table **after changing the reference bandwidth**:

![ 500](../_assets/Pasted%20image%2020260617051648.png)

Now there is only one route to reach `192.168.4.0/24` which is via R2! The cost is also **300** which we calculated before. Also look at destination to `2.2.2.2`, which also have a cost of **101**!

![ 500](../_assets/Pasted%20image%2020260617051921.png)

* You can also configure the cost manually, and will take priority over the automatic cost calculation.
* Use the command `ip ospf cost ?` in interface config mode where ? is the cost to input.

#### Alternative to changing the OSPF cost of an interface

![ 500](../_assets/Pasted%20image%2020260617052340.png)

* One more option to change the OSPF cost of an interface is to change the bandwidth of the interface with the **bandwidth** command.
* For review, the formula to calculate OSPF cost is **reference bandwidth / interface bandwidth**
* Although the bandwidth matches the interface speed by default, changing the interface bandwidth <u>doesn't actually change the speed at which the interface operates</u>.
* The bandwidth is just a value that is used to calculate OSPF cost, EIGRP metric, etc.
* To change the speed at which the interface operates, use the **speed** command.
* Because the bandwidth value is used in other calculations, it is not recommended to change this value to alter the interface's OSPF cost.
* It is recommended that you change the reference bandwidth, and then use the **ip ospf cost** command to change the cost of individual interfaces if you want.

---

### OSPF Cost Summary

Three ways to modify the OSPF cost:

1. Change the **reference bandwidth**:
   `R1(config-router)# auto-cost reference-bandwidth *megabits-per-second*`
1. Manual configuration
   `R1(config-if)# ip ospf cost *cost*`
1. Change the **interface bandwidth** (not recommended):
   `R1(config-if)# bandwidth *kilobits-per-second*`

![ 500](../_assets/Pasted%20image%2020260617052942.png)

There is actually a more convenient way to check the OSPF cost of each interface without being loaded with interface information. Use the command `show ip ospf interface brief`.

---

### OSPF Neighbors

![ 500](../_assets/Pasted%20image%2020260617053250.png)

* Making sure that routers successfully becomes OSPF neighbors is the main task in configuring and troubleshooting OSPF. Once routers become neighbors, they automatically do the work of sharing network information, calculating routes, etc.
* When OSPF is activated on an interface, the router starts sending OSPF **hello** messages out of the interface at regular intervals (determined by **hello timer**). These are used to introduce the router to potential OSPF neighbors.
* The default hello timer is **10 seconds** on an Ethernet connection.
* Hello messages are multicast to `224.0.0.5` (multicast address for all OSPF routers)
* OSPF messages are encapsulated in an IP header, with a value of **89** in the `Protocol` field.

---

### OSPF Neighbor States

#### Down State

![ 500](../_assets/Pasted%20image%2020260617060458.png)

* OSPF is activated on R1's G0/0 interface (let's assume OSPF is activated on R2's G0/0 interface)
* It sends an OSPF hello message to 224.0.0.5. Since R1 doesn't know about R2 yet, the Neighbor ID will be `0.0.0.0`
* It doesn't know about any OSPF neighbors yet, so the current neighbor state is **Down**.

#### Init State

![ 500](../_assets/Pasted%20image%2020260617060934.png)

* When R2 receives the Hello packet, it will add an entry for R1 to its OSPF neighbor table.
* In R2's neighbor table, the relationship with R1 is now in the **Init** state.
* **Init** state = Hello packet received, but own router ID is not in the Hello packet

#### 2-Way State

![ 500](../_assets/Pasted%20image%2020260617061218.png)

* R2 will send a Hello packet containing the RID of both routers. R1 will insert R2 into its OSPF neighbor table in the **2-way** state.
* R1 will send another Hello message, this time containing R2's RID. Now both routers are in the **2-Way** state.
* The 2-way state means the router has received a Hello packet with its own RID in it.
* If both routers reach the 2-way state, it means that all of the conditions have been met for them to become OSPF neighbors. They are now ready to share LSAs to build a common LSDB. If the 2-way state fails, you have to look at issue and troubleshoot it.
* In some network types, a DR (Designated Router) and BDR (Backup Designated Router) will be elected at this point. We will learn abut OSPF network types and DR/BDR elections later.

#### Exstart State

![ 500](../_assets/Pasted%20image%2020260617062022.png)

* The two routers will now prepare to exchange information about their LSDB.
* Before that, they have to choose which one will start the exchange. They do this in the **Exstart** state.
* The router with the higher RID will become the **Master** Router and initiate the exchange. The router with the  lower RID will become the **Slave** Router.
* To decide the Master and Slave, they exchange **DBD** (Database Description) packets.

#### Exchange State

![ 500](../_assets/Pasted%20image%2020260617062328.png)

* In the **Exchange** state, the routers exchange DBDs which contain a list of the LSAs in their LSDB.
* These DBDs *do not include* detailed information about the LSAs, just basic information.
* The router compare the information in the DBD they received to the information in their own LSDB to determine which LSAs they must receive from their neighbor.

#### Loading State

![ 500](../_assets/Pasted%20image%2020260617063223.png)

* In the **Loading** state, routers send *Link State Request* (LSR) messages to request that their neighbors send them any LSAs they don't have.
* The neighbor router will then reply. LSAs are sent in *Link State Update* (LSU) messages.
* The routers send *LSAck* messages to acknowledge that they received the LSAs.

#### Full State

![ 500](../_assets/Pasted%20image%2020260617063621.png)

* In the **Full** state, the routers have a full OSPF adjacency and identical LSDBs.
* They continue to send and listen for Hello packets (every **10 seconds** by default) to maintain the neighbor adjacency.
* Every time a Hello packet is received, the **Dead timer** (**40 seconds** by default) is reset.
* If the Dead timer counts down to 0 and no Hello message is received, *the neighbor is removed*.
* The routers will continue to share LSAs as the network changes to make sure each router has a complete and accurate map of the network (LSDB).

#### OSPF Neighbors summary

![Pasted image 20260617064031.png](../_assets/Pasted%20image%2020260617064031.png)

In OSPF, there are three main steps in the process of sharing LSAs and determining the best route to each destination in the network.

1. **Become neighbors** with other routers connected to the same segment.
1. **Exchange LSAs** with other neighbor routers.
1. **Calculate the best routes** to each destination, and insert them into the routing table.

So here is the more detailed picture when routers become neighbors and when they exchange LSAs:

![Pasted image 20260617064354.png](../_assets/Pasted%20image%2020260617064354.png)

---

### OSPF Message Types Chart

![ 600](../_assets/Pasted%20image%2020260618045103.png)

---

### OSPF `show` Commands

![ 250](../_assets/Pasted%20image%2020260618045233.png)

![Pasted image 20260618045505.png](../_assets/Pasted%20image%2020260618045505.png)

Let's study the different information from `show ip ospf neighbor`:

* The `State` says `FULL/DR` for both neighbors. This means full state and use DRs. We will learn more about DRs later.
* The `Dead Time` caps at 40, but will reset if it receives another hello packet from neighbor router. The routers send hello packets every 10 seconds so it usually counts down to 30 and resets again.

![ 500](../_assets/Pasted%20image%2020260618045803.png)

Now let's look at `show ip ospf interface g0/0` in R1:

![ 500](../_assets/Pasted%20image%2020260618045858.png)

You can see the default hello packets and dead timers. Hello packets send every 10 seconds and the dead timer is 40 seconds.

![Pasted image 20260618050038.png](../_assets/Pasted%20image%2020260618050038.png)

Hello due in 7 seconds means that R1 will send hello packets out of its interface in 7 seconds as it does every 10 seconds.

![Pasted image 20260618050129.png](../_assets/Pasted%20image%2020260618050129.png)

R1 only has one neighbor connected through its `G0/0` interface, which is R2. We will learn about the difference between neighbors and adjacent neighbors later.

![Pasted image 20260618050315.png](../_assets/Pasted%20image%2020260618050315.png)

This means R1 is adjacent with neighbor R2 which is a designated router.

---

### OSPF Configuration (Continuation)

![ 250](../_assets/Pasted%20image%2020260618050452.png)

![Pasted image 20260618050554.png](../_assets/Pasted%20image%2020260618050554.png)

* Instead of using `network` command and calculating the wildcard mask to determine which interface to enable OSPF on, you can directly configure them on the interface.
* Make sure that you are in interface configuration mode, then enter the command `ip ospf *process-id* area *area*`. Now we can enable OSPF on those interfaces without entering OSPF configuration mode.

![ 500](../_assets/Pasted%20image%2020260618050814.png)

* You can also configure ALL interfaces as OSPF passive interfaces by default.
* Use the command (inside OSPF configuration mode) `passive-interface default` to enable passive interface on all interfaces.
* Then to configure specific interfaces as active, use the command `no passive-interface` *interface-id* (while in OSPF configuration mode).
* These can be useful if you have a lot of passive interfaces you need to configure. Though in other case using `passive-interface *interface-id*` might be faster.

![ 500](../_assets/Pasted%20image%2020260618051154.png)

If you configured OSPF directly on interfaces, you'll see slightly different output from `show ip protocols`.

![ 500](../_assets/Pasted%20image%2020260618051358.png)

The `Routing for Networks` is empty. But there is a new information `Routing on Interfaces Configured Explicitly (Area *area*)` which lists all the interfaces we directly configured OSPF on. The other information of `show ip protocols` is the same.

---

### Things we covered

![Pasted image 20260618051650.png](../_assets/Pasted%20image%2020260618051650.png)
![Pasted image 20260618051720.png](../_assets/Pasted%20image%2020260618051720.png)
![Pasted image 20260618051833.png](../_assets/Pasted%20image%2020260618051833.png)
