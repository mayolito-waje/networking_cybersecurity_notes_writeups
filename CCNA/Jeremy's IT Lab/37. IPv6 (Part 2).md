### EUI-64

![ 200](../_assets/Pasted%20image%2020260629045604.png)

* EUI stands for **Extended Unique Identifier**.
* To be more precise, the technically correct term when we are working with IPv6 is Modified EUI-64.
* (Modified) EUI-64 is a method of converting a MAC address (48 bits) into a 64-bit interface identifier.
* This interface identifier can then become the **host portion** of a `/64` IPv6 address.

**How to convert the MAC address to EUI-64 interface identifier:**

1. Divide the MAC address in half
   `1234 5678 90AB` **-->** `1234 56 | 78 90AB`

1. Insert `FFFE` in the middle
   `1234 56FF FE78 90AB`

1. Invert the 7th bit. Every digit in hexadecimal is 4 bits, so the 7th bit is in the 2nd digit from the left.
   `1*2*34 56FF FE78 90AB` **-->** `1*0*34 56FF FE78 90AB`
   
   **EXPLANATION:** Hex `2` is equal to `0b0010` in binary. Since the hex digit is placed 2nd from the left in the original hexadecimal, the first bit of the converted binary of hex digit is the 5th bit in the original hex, which makes the 7th bit equal to `1`. After inverting the 7th bit, `0b0010` will become `0b0000` which equals to `0` in hex.
   
   Now using the mentioned rule, the original hex `1234 56FF FE78 90AB` will become **`1034 56FF FE78 90AB`** after inverting the 7th bit.

Here are some examples of MAC addresses converted to EUI-64 interface identifier:

![ 500](../_assets/Pasted%20image%2020260629051749.png)

---

### EUI-64 Configuration

Now let's look at commands on how to configure EUI-64 on interfaces:

![ 500](../_assets/Pasted%20image%2020260629051830.png)

* In interface config mode, use the command `ipv6 address *prefix* eui-64` to configure the IPv6 on interface using the EUI-64 as the host portion.
* For example, if the subnet uses an address/prefix of `2001:db8:0:1::/64`, to configure the IPv6 using EUI-64 as host, the command will be:
  `R1(config-if)# ipv6 address 2001:db8:0:1::/64 eui-64`

Now, to demonstrate, let's look at MAC address of each interface:

![ 500](../_assets/Pasted%20image%2020260629052454.png)

Now after configuring the IPv6 using modified EUI-64 as host, the IPv6 address of each interface will be:

![ 500](../_assets/Pasted%20image%2020260629053126.png)

If you apply what we've learned on converting the MAC address to EUI-64 interface identifier, the result is the same.

![Pasted image 20260629053015.png](../_assets/Pasted%20image%2020260629053015.png)

Notice that hex `C` is converted to `E` after flipping the 7th bit, and `FFFE` is inserted in the middle of host portion.

---

### EUI-64: Why convert the 7th bit?

* MAC addresses can be divided into two types:
  - **UAA** (Universally Administered Address): Uniquely assigned to the device by the manufacturer
  - **LAA** (Locally Administered Address): Manually assigned by an admin (with the `mac-address` command on the interface) or protocol. Doesn't have to be globally unique.
* You can identify a UAA or LAA by the 7th bit of the MAC address, called the U/L bit (Universal/Local bit):
  - U/L bit set to **0** = **UAA**
  - U/L bit set to **1** = **LAA**
* In the context of IPv6 addresses/EUI-64, the meaning of the U/L bit is reversed:
  - U/L bit set to **0** = The MAC address the EUI-64 interface ID was made from was an **LAA**
  - U/L bit set to **1** = The MAC address the EUI-64 interface ID was made from was a **UAA**

Google **"eui-64 packetlife.net"** for more in-depth explantion.

---

### Global Unicast Addresses

EUI-64 isn't a type of IPv6 address. It is one of the methods of automatically generating an IPv6 using a network prefix and a MAC address.

**Global Unicast Address**, however, is a type of IPv6 address.

* **Global unicast** IPv6 addresses are public addresses which can be used over the Internet.
* Must register to use them. Because they are public addresses, it is expected that they are globally unique.
* Originally defined as the `2000::/3` block (`2000::` to `3FFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF`).
* Now, it is defined as all addresses which aren't reserved for other purposes.

Here is an example of Global Unicast Address:

![ 500](../_assets/Pasted%20image%2020260629061226.png)

---

### Unique Local Addresses

* **Unique local** IPv6 addresses are *private* addresses which **cannot be used over the Internet.**
* You do not need to register to use them. They can be used freely within internal networks and don't need to be globally unique (\*). Can't be routed over the internet.
* Uses the address block `FC00::/7` (`FC00::` to `FDFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF`)
* However, a later update requires the 8th bit set to **1**, so the first two digits must be `FD`.
* \*The global ID should be unique so that addresses don't overlap when companies merge.

An example of Unique Local Address:

![ 500](../_assets/Pasted%20image%2020260629061905.png)

---

### Link Local Addresses

* **Link-local** IPv6 addresses are automatically generated on IPv6-enabled interfaces.
* Use command `R1(config-if)# ipv6 enable` on an interface to enable IPv6 on an interface. It will automatically generate link local address which will be the only IPv6 address on the interface if you didn't set a configured IPv6.
* Uses the address block `FE80::/10` (`FE80::` to `FEBF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF`)
* However, the standard states that the 54 bits after `FE80/10` should be all **0**, so you won't see link local addresses beginning with `FE9`, `FEA`, or `FEB`. Only `FE8`.
* The interface ID is generated using EUI-64 rules.

![ 500](../_assets/Pasted%20image%2020260629062432.png)

If you configured the IPv6 using EUI-64 as the host, notice that the host portion of link local addresses are also the same.

* *Link-local* means that these addresses are used for communication within a single link (subnet). Routers **will not** route packets with a link-local destination IPv6 address.
* Common uses of link-local addresses:
  - routing protocol peerings (OSPFv3 uses link-local addresses for neighbor adjacencies)
  - next-hop addresses for static routes
  - *Neighbor Discovery Protocol* (NDP, IPv6's replacement for ARP) uses link-local addresses to function

Let's show an example of one use for link local addresses:

![ 500](../_assets/Pasted%20image%2020260629062827.png)

Let's say PC1 wants to send a packet to PC2 which is from a different subnet:

![ 500](../_assets/Pasted%20image%2020260629062914.png)
![ 500](../_assets/Pasted%20image%2020260629063045.png)

Routers can use link-local addresses to determine the next hop for the packet to reach the destination without the need of configuring global unique addresses to determine which router to forward the packet to. However you can't ping another router's link-local address, the packet will simply be dropped.

---

### Multicast Addresses

* **Unicast** addresses are one-to-one communication: one source to one destination.
* **Broadcast** addresses are one-to-all communication: one source to all destination (within the subnet).
* **Multicast** addresses are one-to-many: one source to multiple destinations (that have joined the specific *multicast* group).
* IPv6 uses range `FF00::/8` for multicast. (`FF00::` to `FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF:FFFF`)
* **IPv6 doesn't use broadcast** (there is no 'broadcast address' in IPv6!)

Here are some examples of important multicast addresses, both for IPv6 and IPv4 versions.

![ 500](../_assets/Pasted%20image%2020260629063602.png)

To easily remember them, look at the last digit of IPv6 and the last octet of IPv4. Both uses the same values. If you memorize the multicast addresses for IPv4, you also almost memorize the multicast addresses for IPv6, but this time using a format of `FF02::<last-digit-in-hex>`.

Since IPv6 doesn't have broadcast, it uses **all nodes/hosts** multicast address which functions like a broadcast since its destined for *all hosts* like a broadcast.

---

### Multicast Address Scopes

* IPv6 defines multiple mulsticast 'scopes' which indicate how far the packet should be forwarded.
* The addresses in the previous slide all use the 'link-local' scope (`FF02`), which stays in the local subnet.
* IPv6 multicast scopes:
  - **Interface-local** (`FF01`) : The packet doesn't leave the local device. Can be used to send traffic to a service within the local device.
  - **Link-local** (`FF02`): The packet remains in the local subnet. Routers will not route the packet between subnets.
  - **Site-local** (`FF05`): The packet can be forwarded by routers. Should be limited to a single physical location (not forwarded over a WAN)
  - **Organization-local** (`FF08`): Wider in scope than site-local (an entire company/organization).
  - **Global** (`FF0E`): No boundaries. Possible to be routed over the Internet.

![Pasted image 20260630045459.png](../_assets/Pasted%20image%2020260630045459.png)

The picture above is demonstrated on how multicast scopes look like in a network infrastructure.

Let's look again at the interface information after we configured the EUI-64 IPv6 address using the command `show ipv6 interface g0/0`:

![ 500](../_assets/Pasted%20image%2020260630050143.png)

In the boxed section, it lists the multicast groups the interface is joined to.

* You can see that it is a member of **all nodes** multicast group (`FF02::1`) in `2001:DB8::/64` subnet.
* The interface is also a member of **all routers** multicast group (`FF02::2`).
* We will learn about the third multicast address later.

---

### Anycast Addresses

* **Anycast** is a new feature of IPv6.

* Anycast is 'one-to-one-of-many'. Below is the picture that demonstrates Anycast address via Wikipedia example:
  
  ![113](../_assets/Pasted%20image%2020260630050952.png)

* Multiple routers are configured with the same IPv6 address.
  - They use a routing protocol to advertise the address.
  - When hosts sends packets to that destination address, routers will forward it to the nearest router configured with that IP address (based on routing metric).

* There is no specific address range for anycast addresses. Use a regular unicast address (global unicast, unique local) and specify it as an anycast address:
  `R1(config-if)# ipv6 address 2001:db8:1:1::99/128 anycast`

Here is the example CLI for configuring anycast address.

![ 500](../_assets/Pasted%20image%2020260630051323.png)

In `show ipv6 interface *interface*` command, you can see that the interface have two global unicast address: one address configured using EUI-64 and one anycast address:

![ 500](../_assets/Pasted%20image%2020260630051524.png)

You can tell which is EUI-64 and anycast address through the bracket (`[TYPE]`).

---

### Other IPv6 Addresses

* `::` = The *unspecified* IPv6 address
  - Can be used when a device doesn't yet know its IPv6 address.
  - IPv6 default routes are configured to `::/0`
  - IPv4 equivalent: `0.0.0.0`

* `::1` = The loopback address
  - Used to test the protocol stack on the local device.
  - Messages sent to this address are processed within the local device, but not sent to other devices.
  - IPv4 equivalent: `127.0.0.0/8` address range.

---

### Things we covered

![ 500](../_assets/Pasted%20image%2020260630052143.png)
