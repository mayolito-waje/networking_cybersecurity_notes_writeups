* FTP (File Transfer Protocol) and TFTP (Trivial File Transfer Protocol) are industry standard protocols used to transfer files over a network.
* They both use a client-server model.
  - Clients can use FTP or TFTP to copy files from a server
  - Clients can use FTP or TFTP to copy files to a server
* As a network engineer, the most common use for FTP/TFTP is in the process of upgrading the operating system of a network device.
* You can use FTP/TFTP to download the newer version of IOS from a server, and then reboot the device with the new IOS image.

We'll use the below example to understand FTP and TFTP.

![Pasted image 20260719103750.png](../_assets/Pasted%20image%2020260719103750.png)

---

### TFTP

* TFTP was first standardized in 1981.
* Named 'Trivial' because it is simple and has only basic features compared to FTP.
  - TFTP only allows a client to copy a file to or from a server.
* Was released after FTP, but is **not** a replacement for FTP. It is another tool to use when lightweight simplicity is more important than functionality.
* No authentication (username/PW) is needed, so servers will respond to all TFTP requests.
* TFTP have no encryption, so all data is sent in **plain text**.
* Because of lack of security, TFTP is best used in a controlled environment to transfer small files quickly. It is not recommended to transfer important files over the internet.
* TFTP servers listen on **UDP port 69**.
* UDP is connectionless and doesn't provide reliability with retransmissions. However, TFTP has similar built-in features within the protocol itself.

---

### TFTP Reliability

![Pasted image 20260719104544.png](../_assets/Pasted%20image%2020260719104544.png)

* Every TFTP data messaged is **acknowledged**.
  - If the client is transferring a file to the server, the server will send Ack messages.
  - If the server is transferring a file to the client, the client will send Ack messages.
* Timers are used, and if an expected message isn't received in time, the waiting device will re-send its previous message.

---

### TFTP Connections

![Pasted image 20260719105003.png](../_assets/Pasted%20image%2020260719105003.png)

---

### TFTP ID

![Pasted image 20260719105150.png](../_assets/Pasted%20image%2020260719105150.png)

* When the client sends the first message to the server, the destination port is UDP 69 and the source is a random ephemeral port.
* This random port is called a 'Transfer Identifier' (ID) and identifies the data transfer.
* The server then also selects a random TID to use as the source port when it replies, **not 69**.
* When the client sends the next message, the destination port will be the server's TID, **not 69**.
* \*==This is beyond the scope of the CCNA, but is an interesting part of TFTP's operation.==

---

### File Transfer Protocol

* FTP was first standardized in 1971.
* FTP uses **TCP ports 20 and 21**.
* Usernames and passwords are used for authentication, however there is no encryption. Everything is sent in plain text.
* For greater security, **FTPS (FTP over SSL/TLS)** can be used. ==FTPS is an upgrade to FTP==.
* **SSH File Transfer Protocol (SFTP)** can also be used for greater security. ==SFTP is a different protocol, not an upgrade. It just have a similar name to FTP==.
* FTP is more complex than TFTP and allows not only file transfers, but clients can also navigate file directories, add and remove directories, list files, etc.
* The client sends *FTP commands* to the server to perform these functions.
  Go here for list of FTP commands: https://en.wikipedia.org/wiki/List_of_FTP_commands

---

### FTP Control and Data Connections

#### FTP Control Connections

* FTP uses two types of connections:
  - An **FTP control** connection (**TCP 21**) is established and used to send TFTP commands and replies.
  - When files or data are to be transferred, separate **FTP data** (**TCP 20**) connections are established and terminated as needed.

![Pasted image 20260719110341.png](../_assets/Pasted%20image%2020260719110341.png)

#### Active Mode FTP Data Connections

* The default method of establishing FTP data connections is **active mode**, in which the ==server initiates the TCP connection==.

![Pasted image 20260719110538.png](../_assets/Pasted%20image%2020260719110538.png)

#### Passive Mode FTP Data Connections

* In FTP **passive mode**, the client initiates the data connection. This is often necessary when the client is behind a firewall, which could block the incoming connection from the server.
* A point to remember: ==Firewalls usually don't permit 'outside' devices to initiate connections. In this case, FTP **passive mode** is used and the client (behind the firewall) initiates the TCP connection.==

![Pasted image 20260719110821.png](../_assets/Pasted%20image%2020260719110821.png)

---

### FTP vs TFTP

![Pasted image 20260719111106.png](../_assets/Pasted%20image%2020260719111106.png)

---

### IOS File Systems

* A file system is a way of controlling how data is stored and retrieved.
* You can view the file systems of a Cisco IOS device with **`show file systems`**

![Pasted image 20260719115205.png](../_assets/Pasted%20image%2020260719115205.png)

Next, let's actually learn how to use FTP/TFTP to transfer files into the flash memory of Cisco devices.

---

### Upgrading Cisco IOS

![ 500](../_assets/Pasted%20image%2020260719115414.png)

**Details:**

* R1 is a Cisco Router running Cisco IOS.
* SRV1 contains the updated Cisco IOS which the R1 needs to update its system.
* SRV1 is a server running both TFTP and FTP. We will try both to get the new version from SRV1 into R1.

**Upgrading Cisco IOS**

* You can view the current version of IOS with **`show version`**
  ![Pasted image 20260719120024.png](../_assets/Pasted%20image%2020260719120024.png)
  
  Notice that the Cisco IOS has **K9** in its image name (C2900-UNIVERSAL**K9**-M), meaning it supports cryptographic features such as SSH.

* You can view the contents of flash with **`show flash`**
  ![Pasted image 20260719120057.png](../_assets/Pasted%20image%2020260719120057.png)

---

### Copying Files (TFTP)

**What we're going to do:**

* Copy the new version of IOS from SRV1 to the **flash** of R1 using **TFTP**.
* Configure R1 to boot using new version of IOS.
* Delete the old version from flash.

![Pasted image 20260719122402.png](../_assets/Pasted%20image%2020260719122402.png)

---

### Upgrading Cisco IOS (Continuation)

![Pasted image 20260719122517.png](../_assets/Pasted%20image%2020260719122517.png)

* In `show flash` command, you can see the the file has been successfully copied into R1 from TFTP server.

![Pasted image 20260719122635.png](../_assets/Pasted%20image%2020260719122635.png)

* To actually boot using the new updated IOS, use the command **`boot system *filepath*`**
* Make sure to save the configuration before reloading the device (`write memory`) or the boot system command won't take effect.
* `reload` command restarts the device.

**AFTER UPDATING THE DEVICE:**

![Pasted image 20260719122922.png](../_assets/Pasted%20image%2020260719122922.png)

* `show version` command now shows the updated IOS version.

Now let's delete the old version from flash.

![Pasted image 20260719123034.png](../_assets/Pasted%20image%2020260719123034.png)

* Now the old file is gone after using the command `show flash`.

---

### Copying Files (FTP)

![Pasted image 20260719124536.png](../_assets/Pasted%20image%2020260719124536.png)

* The same commands is be used to boot the new software, save the configuration, and reload the router.

---

### Command Review

![Pasted image 20260719124702.png](../_assets/Pasted%20image%2020260719124702.png)

---

### Things we covered

* The purpose of FTP/TFTP
* FTP/TFTP functions & differences
* IOS File Systems
* Using FTP/TFTP in IOS
